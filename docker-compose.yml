
services:
  api:
    build:
      context: .
      dockerfile: infrastructure/docker/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/agentcloud
      - REDIS_URL=redis://redis:6379/0
      - SECRET_KEY=${SECRET_KEY}
      - CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://frontend:3000,http://localhost:8000
      - RUN_MIGRATIONS=true
      - DEPLOYMENT_MODE=local
      - DEPLOYMENT_REGION=us-east-1
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request, json; resp = urllib.request.urlopen('http://localhost:8000/health_check').read(); status = json.loads(resp); exit(0 if status['status'] == 'ready' else 1)\""]
      interval: 15s
      timeout: 10s
      retries: 25
      start_period: 60s

  worker:
    build:
      context: .
      dockerfile: infrastructure/docker/Dockerfile
    volumes:
      - .:/app
    command: python -m arq app.workers.arq_worker.WorkerSettings
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/agentcloud
      - REDIS_URL=redis://redis:6379/0
      - PYTHONPATH=/app
      - SECRET_KEY=${SECRET_KEY}
    depends_on:
      db:
        condition: service_healthy
      api:
        condition: service_healthy
      redis:
        condition: service_healthy

  db:
    image: pgvector/pgvector:pg15
    environment:
      POSTGRES_DB: agentcloud
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  redis:
    image: redis:7
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
      - API_BASE_URL=http://api:8000
    depends_on:
      api:
        condition: service_healthy

  smoke:
    build:
      context: .
      dockerfile: infrastructure/docker/Dockerfile
    volumes:
      - .:/app
    command: python scripts/smoke.py
    env_file:
      - .env
    environment:
      - API_BASE_URL=http://api:8000
    depends_on:
      api:
        condition: service_healthy
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  doctor:
    build:
      context: .
      dockerfile: infrastructure/docker/Dockerfile
    volumes:
      - .:/app
    command: python scripts/doctor.py --fix
    env_file:
      - .env
    environment:
      - API_BASE_URL=http://api:8000
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/agentcloud
    depends_on:
      api:
        condition: service_healthy
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  postgres_data:
